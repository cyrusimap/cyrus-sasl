C
~~~~~~~~~~
) Work with our GSSAPI libs
) No buffers on the stack
) No fixed-length buffers
) Zero all sensitive data after use
) Better/FASTER random numbers (init time is pretty miserable)
) Test suite
) Configuration directory support -- multiple files which, together,
  determine the security parameters and available mechanisms on a per-protocol
  and per-application basis.

Documentation
~~~~~~~~~~
) Rob:
  - About SASL
) Karen:
  - Java documentation cleanup?
  - SASL home page

Mechs to write
~~~~~~~~~~
) PASSDSS-3DES-1 (draft-newman-sasl-passdss-01.txt)
) OTP

Digest-MD5
~~~~~~~~~~
) Purify, exhaustively test, &c.

ABI
~~~~~~~~~~
) Consider IRIX ABI issues for plugins
	- /usr/lib32/sasl?
	- /usr/lib/sasl/{ABI}?
) Standardize the plugin ABI
